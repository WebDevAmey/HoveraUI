import type { ComponentItem } from "@/types";

// Marquee was retired in the monochrome curation (2026-07-06); the category
// slot is kept so the registry build and nav wiring stay stable if it returns.
export const marquees: ComponentItem[] = [];
